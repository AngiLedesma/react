const EjemploProps1 = props =>{

    return(
        <h3>Hola {props.nombre}</h3>
    )
}
export{
    EjemploProps1
    //aca usamos {} y no export default porque son varios componentens distintos para exportar
}